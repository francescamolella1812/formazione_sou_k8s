apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-example.fullname" . }}
  labels:
    {{- include "flask-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-example.selectorLabels" . | nindent 4 }}
